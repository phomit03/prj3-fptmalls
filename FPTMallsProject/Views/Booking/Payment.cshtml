@*@model PaymentViewModel

@{
    Layout = "_LayoutCinema";
    ViewBag.Title = "Payment";
}

<div class="container my-3">

    <form asp-action="ProcessPayment" method="post">

        <div class="form-group">
            <label for="card-number">Card Number</label>
            <input type="text" class="form-control" id="card-number" name="CardNumber" required>
        </div>

        <div class="form-group">
            <label for="expiration-date">Expiration Date</label>
            <input type="text" class="form-control" id="expiration-date" name="ExpirationDate" required>
        </div>

        <div class="form-group">
            <label for="cvv">CVV</label>
            <input type="text" class="form-control" id="cvv" name="CVV" required>
        </div>

        <button type="submit" class="btn btn-primary">Pay</button>
        <a href="@Url.Action("Index", "Movie")" class="btn btn-secondary">Cancel</a>

    </form>

</div>
*@